require 'spec_helper'

describe NaiveBayesClassifier do
  it 'has a version number' do
    expect(NaiveBayesClassifier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
