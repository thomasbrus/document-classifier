require 'spec_helper'

describe DocumentClassifier do
  it 'has a version number' do
    expect(DocumentClassifier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(true).to eq(true)
  end
end
